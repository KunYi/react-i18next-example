import en from './en/common'; // English translations
import tw from './tw/common'; // Traditional Chinese translations
import cn from './cn/common'; // Simplified Chinese translations

const resources = {
  en: { common: en },
  tw: { common: tw },
  cn: { common: cn },
};

export default resources;
